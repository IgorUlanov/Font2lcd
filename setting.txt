#include <stdint.h>
const uint16_t height_%name%=%size%;
const uint16_t adr_%name%[]= {

#include <stdint.h>
const uint16_t buf_%name%[]= {

Строки до первой пустой строки это голова файла с таблицей адресов символов в массиве.
После пустой строки, следущие строки до следущей пустой строки, голова файла с массивом символов.
Вместо подстроки %name% подставляется имя сформированое из названия шрифта и размера.
Вместо подстроки %size% подставляется заданная высота символов.
